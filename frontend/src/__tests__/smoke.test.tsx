import { describe, it, expect } from 'vitest'

// Simple placeholder test to ensure frontend test pipeline runs.
describe('smoke', () => {
  it('adds numbers correctly', () => {
    expect(1 + 1).toBe(2)
  })
})